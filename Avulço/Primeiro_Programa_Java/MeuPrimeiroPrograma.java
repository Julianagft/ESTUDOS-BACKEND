class MeuPrimeiroPrograma {

    public static void main(String[] args) {
        System.out.println("Hello World");
    }
}

// O método pertence a classe, isto é, ele faz parte do corpo da classe. Por isso, vamos começar a escrever o método dentro das chaves, dentro do corpo da classe.

/*
Esse é o nosso método principal (main). No Java, existe um padrão para escrever esse método.
Primeiro, dizemos que ele é um método público (public), ou seja, todos têm acesso a ele.
Precisamos falar que esse método pertence a classe, por isso, falamos que ele é um método estático (static).
Esse método é não devolve nada, isto é, ele é executado e não retorna nenhum valor após sua execução, indicamos isso com a palavra void
Da mesma forma que as classes, o método precisa de chaves ({ }) para indicar seu corpo.
*/
