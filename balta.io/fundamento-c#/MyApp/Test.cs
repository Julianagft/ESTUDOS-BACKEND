namespace MyApp {
    class Teste {

    }
}

// Não pode ter o mesmo namespace E a mesma classe. Posso repetir o namespace (embora não seja recomendado), mas não a classe.  
// Podemos ter um namespace dentro de outro usando o ponto ".". 