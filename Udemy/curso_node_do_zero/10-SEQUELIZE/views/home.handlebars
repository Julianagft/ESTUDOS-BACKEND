<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <h1>Usuários Cadastrados:</h1>
    <div class="container">
        {{#each users}}
            <div class="item">
                <p>{{this.name}}</p>
                <p>Cargo: {{this.occupation}}</p>
                <p>Newsletter: {{#if this.newsletter}}Inscrito{{else}}Não Inscrito{{/if}}</p>
                <div class="actions">
                    <a href="/users/{{this.id}}">Ver Detalhes</a>
                </div>
            </div>
        {{/each}}
    </div>
</body>
</html>