<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <h1>Usuários Cadastrados:</h1>
    <div class="container">
        {{#each users}}
            <div class="item">
                <p>{{this.name}}</p>
                <p>Cargo: {{this.occupation}}</p>
                <p>Newsletter: {{#if this.newsletter}}Inscrito{{else}}Não Inscrito{{/if}}</p>
                <div class="actions">
                    <a href="/users/{{this.id}}" class="details-btn">Ver Detalhes</a>
                    <a href="/users/edit/{{this.id}}" class="edit-btn">Editar</a>
                    <form action="users/delete/{{this.id}}" method="post">
                        <input type="submit" value="Excluir" class="delete-btn">
                    </form>
                </div>
            </div>
        {{/each}}
    </div>
</body>
</html>